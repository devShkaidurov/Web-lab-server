spring:
  datasource:
    url: jdbc:postgresql://localhost:5552/university
    username: postgres
    password: Ffijvd5555
  jpa:
    generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
security:
  logins: admin,user
  passwords: admin,password
  roles: ADMIN,USER